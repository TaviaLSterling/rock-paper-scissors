function play(playerChoice){
    let compChoice = Math.random();
    if (compChoice > .34) {
        compChoice = "rock"};
        else if(compChoice > .66) {
            compChoice = "paper"
        } else compChoice = "scissors"
        
    }
}